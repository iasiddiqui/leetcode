/*
Approach: Plus One (Iterative Carry Handling)

We start from the least significant digit (end of the array) and add one. If the digit becomes 10, we set it to 0 and carry over to the next digit. This continues until no carry remains. If all digits become 0 (like [9,9,9] → [0,0,0]), we add a 1 at the beginning to represent the new number.

Time Complexity: O(n){ we traverse the digits once.}
Space Complexity: O(1){ modifies the array in place except for one extra digit in rare cases.}
*/

/**
 * @param {number[]} digits
 * @return {number[]}
 */
var plusOne = function(digits) {
    for (let i = digits.length - 1; i >= 0; i--) {
        if (digits[i] < 9) {
            digits[i]++;
            return digits;
        }
        digits[i] = 0;
    }
    digits.unshift(1);
    return digits;
};
